from src.core.helpers import get_file_size


def validate_title(title: str) -> list:
    errors = []

    return errors


def validate_authors(authors: str) -> list:
    errors = []

    return errors


def validate_year(year) -> list:
    errors = []

    return errors


def validate_pdf(file) -> list:
    errors = []

    return errors
